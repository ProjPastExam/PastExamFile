/// @description Insert description here
// You can write your code in this editor
isCol = false;
//if (global.checkUse == 2) instance_destroy();

with (ob_roomControl)
{
	other.costIndex = (25 + stage*25) * (1 + global.checkNum);
}
